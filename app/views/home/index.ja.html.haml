:markdown
  # ようこそ、#{app_name} へ!!!

  **#{app_name}** は、何かについて、話したり、ぐだったりするサービスです。

-#
  &raquo;
  = link_to '詳しく見る', guidance_path
  &laquo;



%h2= t :channels

- if logged_in?
  %ul
    %li= link_to t(:new_channel), new_channel_path

%table
  - Channel.all.each do |channel|
    %tr
      %td= channel.name
      %td= link_to 'Show', channel
      %td= link_to 'Edit', edit_channel_path(channel)
      %td= link_to 'Destroy', channel, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br
